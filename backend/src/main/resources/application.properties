# --- Server ---
server.port=8080

# --- Database Configuration (MySQL 127.0.0.1:3306, user=root) ---
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/finsync_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=${MYSQL_PASSWORD:Sathya@13}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT ---
finsync.jwt.secret=ChangeThisSecretKeyToSomethingLongAndRandomForProductionUse123456
finsync.jwt.expiration-ms=86400000
